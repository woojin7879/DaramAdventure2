# 다람이의 모험 2 — UI 디자인 기준

## 방향

Hallmark의 playful 장르를 기존 숲 세계관에 적용한다. 숲 그림은 메인과 전투의 주인공으로 남기고, 도구와 기록은 차분한 모험 도감처럼 읽힌다. 한국어 중심. 전투 중 필요한 정보는 짧게, 선택과 기록에서는 근거를 충분히 보여준다.

## 화면별 구조

- `index.html` 메인: 왼쪽 게임 제목과 숲 삽화, 오른쪽 모험 선택. 연차를 이름이 있는 목록으로 표시한다. 헬 모드 해금 조건과 저장한 계절 이어하기를 유지한다. 관리자 링크는 노출하지 않는다.
- 전투 HUD: 상단 체력·계절 시간·처치, 얇은 경험치 선, 하단 무기 6칸과 패시브. 게임 영역은 화면 높이를 활용한다.
- 강화: 따뜻한 종이색 도감. 아이콘·무기명·효과·레벨을 세로 선택 목록으로 비교한다. 숫자 1~3 단축키 유지.
- 일시정지/조작법: 좁은 판형, 왼쪽 정렬, 직접적인 버튼 이름.
- 결과: 생존 시간/처치/레벨을 먼저, 무기별 피해 표와 성장 기록을 이어서 읽는다. 작은 화면에서는 표 내부만 가로 스크롤한다.
- 시네마틱: 기존 전체 화면 삽화와 대사 연출 유지. 화자, 자막, 재생 조작의 대비를 공통 색으로 조정한다.
- `lab.html`, `boss.html`: 테스트 작업대. 이미지가 있는 무기 목록, 값 조절, 캔버스와 실측 통계.
- `admin.html`: 테스트 경로 목록. 별도의 홍보형 카드나 부가 기능을 넣지 않는다.

## 공통 토큰

`src/ui-tokens.css`가 색·글꼴·간격·동작의 기준이다. 어두운 소나무색 배경, 따뜻한 종이색 글자, 도토리색 선택 강조. 강화창은 종이색 바탕과 짙은 갈색 글자를 사용한다. 무기 고유색은 전투/통계 구분용으로 유지한다.

제목은 한국어 명조 계열, 본문과 게임 로고는 시스템 한글 고딕, 숫자 일부는 고정폭. 외부 폰트 요청 없이 브라우저와 Electron 오프라인에서 동작한다. 제목은 기울이지 않는다.

간격: 4/8/12/16/24/32/48/64px. 버튼 모서리 3px. 조작 버튼은 원칙적으로 높이 44px 이상. 짧은 상태 변화 150ms; 과도한 확대나 떠오르는 효과는 쓰지 않는다. 기존 게임 인트로의 숫자 착지 연출은 게임 콘텐츠로 유지한다. 동작 줄이기 설정 존중.

## 아이콘

무기 14종과 패시브 6종은 생성한 5×4 이미지 시트 하나를 공유한다. 원본 그림은 교체하거나 문자로 재현하지 않고 `src/item-icons.js`의 좌표로 선택한다. 아이콘 옆 이름·레벨 및 접근성 설명을 유지한다. 음량은 확실한 파형/취소선을 가진 SVG 스피커 아이콘과 켜짐/꺼짐 표시를 공통 사용한다.

## 상태 및 반응형

키보드 포커스는 즉시 보이는 외곽선. 잠긴 연차는 '잠김' 문구와 aria-disabled를 함께 표시한다. 비활성 음량은 취소선과 접근성 이름, 오류 시 사용 불가 표시. 저장/해금/실험실 기능은 기존 동작을 유지한다.

320/375/414px에서는 한 열 메뉴와 작은 도감 아이콘, 768px에서는 좁은 HUD, 큰 화면에서는 2열 메인. 짧은 가로 화면의 모달은 세로 스크롤을 허용한다. 기존 모바일 CSS의 절대 위치 아이콘/숨겨진 설명 규칙을 명시적으로 해제한다.

## 적용 검토

소스 검토: P4 H4 E4 S5 R4 V4. 전용 삽화, 한국어 항목명, 도감/전투/기록별 구조 차이로 일반 웹 템플릿의 반복을 줄였다. 브라우저 자동 시각 검사는 이번 작업에서 실행하지 않았으며 실제 화면 검증 결과로 간주하지 않는다.
